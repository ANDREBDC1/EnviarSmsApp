import React, { useEffect, useContext } from 'react';
import { Container, CustomText} from './styles'

export default () =>{

    return (
         <Container>
              <CustomText>Dispositivo Registrado</CustomText>
        </Container>
        )
}
